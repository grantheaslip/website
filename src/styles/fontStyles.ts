import { css } from "@emotion/react";

export const fontSansSerif = css({
  fontFamily: "ui-sans-serif, system-ui, sans-serif",
});

export const fontSerif = css({
  fontFamily: "ui-serif, Georgia, sans-serif",
});
